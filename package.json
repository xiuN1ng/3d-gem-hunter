{
  "name": "gem-hunter-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "test": "node --test",
    "build": "vite build",
    "check:bundle": "node scripts/check-build-budget.mjs",
    "validate": "npm test && npm run build && npm run check:bundle",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "vite": "8.1.4"
  }
}
